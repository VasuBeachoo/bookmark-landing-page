import styled from "styled-components";

export const TabOption = styled.p``;

export const TabOptionBox = styled.div``;

export const TabBox = styled.div``;

const Tab = ({ className }) => {
  return <TabBox className={className}></TabBox>;
};

export default Tab;
